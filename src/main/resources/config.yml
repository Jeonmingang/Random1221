# UltimateRandomRoulette (1.21.1 / Java 21) default config

messages:
  prefix: "&b[룰렛]&r "
  created: "&a룰렛 &e%key%&a 가 생성되었습니다."
  exists: "&c이미 존재하는 키입니다: &e%key%"
  deleted: "&a룰렛 &e%key%&a 를 삭제했습니다."
  not_found: "&c해당 키의 룰렛이 없습니다: &e%key%"
  set_item_bind: "&a현재 손에 든 아이템을 &e%key%&a 룰렛 열쇠로 지정했습니다. (우클릭 시 뽑기)"
  need_item_in_hand: "&c손에 든 아이템이 없습니다."
  open_preview: "&a확률/구성 미리보기: &e%key%"
  draw_start: "&a룰렛이 회전합니다!"
  draw_win: "&a축하합니다! &e%item%&a 를 획득했습니다!"
  no_items: "&c설정된 아이템이 없습니다. /random add <키> <가중치> 로 등록하세요."
  need_key: "&c키가 필요합니다. 손/인벤에 &e%key%&c 키를 1개 이상 가지고 계셔야 합니다."

# 뽑기 키 아이템을 우클릭해야만 뽑기가 가능하게 하려면 true
need-key: false

titles:
  preview: "구성 미리보기: %key%"
  spinner: "룰렛 뽑기: %key%"
  package-claim: "패키지: %name%"

preview:
  show-weight: true
  show-chance: true
  weight-format: "&7가중치: &e%weight%"
  chance-format: "&7확률: &e%chance%%"
  remove-tags: [ "귀속", "내구도" ]

spinner:
  glass: "EMERALD_BLOCK"
  glass-name: "&f"
  glow: true
  use-pointer-panes: true
  pointer-name: "&6▲ 중앙 슬롯 ▼"

package:
  claim-button-name: "&a[ 보상 수령 ]"
  claim-button-lore:
    - "&7아래 보상 아이템이 지급됩니다."
    - "&f인벤토리가 가득차면 바닥에 드롭됩니다."

randomkey:
  key-header: "&6&l[ 뽑기 ] &e%key%"
  key-subtitle: "&8━ &7뽑기 구성 (확률)"
  key-entry: "&7- &f%item% &7(&e%chance%%%&7)"
  key-footer: "&8우클릭 시 뽑기 GUI"
